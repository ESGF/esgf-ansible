pkgs:
  apache: httpd
  postgres_server: postgresql-server-8.4.20
  postgres_devel: postgresql-devel-8.4.20
  psgycopg2: python-psycopg2

conda:
  prefix: /usr/local/conda
  exe: "{{ conda_prefix }}/bin/conda"
  actv: "source {{ conda_prefix }}/bin/activate"

db:
  users:
    super: 
      name: dbsuper
      pass: foobar
    pub:
      name: esgcet
      pass: foobar